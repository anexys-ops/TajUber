# Copier vers deploy/env.prod et éditer (ne jamais committer env.prod)
POSTGRES_USER=taj
POSTGRES_PASSWORD=changez_moi_fort
POSTGRES_DB=taj

JWT_SECRET=changez_moi_minimum_32_caracteres_aleatoires

# URL publiques (cohérentes avec Nginx)
NEXT_PUBLIC_API_URL=https://taj.apps-dev.fr/api
NEXT_PUBLIC_WEB_POS_URL=https://taj.apps-dev.fr/pos
NEXT_PUBLIC_TENANT_SLUG=taj-poulet-demo

CORS_ORIGINS=https://taj.apps-dev.fr

# APP_VERSION : ne pas la fixer ici en général — le déploiement exporte
# APP_VERSION depuis version.txt (voir deploy/DEPLOY.fr.md).

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PRINT_AGENT_SECRET=
