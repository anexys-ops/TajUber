# Base de données (docker-compose : voir docker-compose.yml)
DATABASE_URL="postgresql://taj:taj@localhost:5432/taj?schema=public"

# API Nest
PORT=3001
CORS_ORIGINS=http://localhost:3040,http://localhost:3041
JWT_SECRET=change-me-in-production
PRINT_AGENT_SECRET=print-dev-secret

# Back-office Next (SSR) — jeton plateforme pour appeler GET /platform/tenants
TAJ_PLATFORM_TOKEN=

# Stripe (compte plateforme) — Connect + Billing
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Next.js (optionnel)
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_TENANT_SLUG=taj-poulet-demo
NEXT_PUBLIC_WEB_POS_URL=http://localhost:3041

# Seed Prisma
SEED_PASSWORD=ChangeMeDev123!
